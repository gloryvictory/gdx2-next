# HOST_BCK=r48-vws03.zsniigg.local

HOST_BCK=localhost
PORT_BCK=8001

GDX2MAP_SERVER_HOST=localhost
GDX2MAP_SERVER_PORT=8002

# HOST_DB_BCK=localhost
# HOST_DB_DB=gdx2map
# HOST_DB_USER=gdx2map
# HOST_DB_PASSWORD=gdx2mappwd
# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema
# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings
# DATABASE_URL=`postgresql://${HOST_DB_USER}:${HOST_DB_PASSWORD}@${HOST_DB_BCK}:5432/${HOST_DB_DB}?schema=${HOST_DB_DB}`
# DATABASE_URL=`postgresql://gdx2map:gdx2mappwd@localhost:5432/gdx2map?schema=gdx2map`
